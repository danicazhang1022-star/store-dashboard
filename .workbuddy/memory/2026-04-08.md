# 2026-04-08 工作日志

## 图片上传问题根因定位
- **写入格式错误**：之前v3版本将图片字段改为纯字符串 `"产品图片": image_id`，但正确格式应为 `[{"image_id": image_id}]`（数组格式）
  - 修复来源：`~/.workbuddy/skills/腾讯文档/references/smartsheet_references.md` 第824行和第1050行
  - 已修复 `upload_images_to_smartsheet.py` 的 `update_record_image` 函数
- **image_id 有效期仅一天**：`upload_image` 返回的 image_id 有效期为24小时，过期后图片不再显示
- **API 仍限流**：错误码 400007，暂时无法执行上传

## 销售仪表盘
- 创建了 `dashboard.html`，包含今日/本周/本月/款式排行四个Tab
- 目前使用模拟数据，等限流恢复后连接真实数据
- 用户希望手机上也能看，最终方案是在腾讯文档智能表格中做仪表盘

## 薪资自动计算
- 更新了 `auto_salary.py`，支持从每日销售表自动汇总到薪资计算表
- 阶梯提成：≤3000→2%, ≤6000→2.5%, ≤10000→3%, >10000→4%
- 大单奖励：≥1000→¥10, ≥2000→¥20, 每人每天最多计2笔
- 日薪：新手期¥260, 稳定期¥280, 熟练期¥300

## 每日销售表结构优化
- 确认采用「每件衣服一行」模式（便于查找引用和库存管理）
- **新增「订单号」字段**：用于大单奖励计算（同一单多件衣服填同一订单号）
- 大单奖励按订单号汇总金额后判断，不再按单件金额
- 日期字段建议设默认值「今天」，新增记录时自动填当天
- 待做：限流恢复后在智能表中添加订单号字段、测试脚本
